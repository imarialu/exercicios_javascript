/* Declare uma variável chamada "númeroMáximo", atribua a ela o valor máximo que um número pode ter em javaScript 
   e imprima o valor no console. 
*/

let numeroMaximo = Number.MAX_VALUE; // Propriedade que informa o número máximo que pode ser representado pela linguagem

console.log("Número Máximo: ", numeroMaximo);